apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: teslamate-grafana-data
  labels:
    app: teslamate-grafana
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
