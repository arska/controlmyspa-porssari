apiVersion: v1
kind: ConfigMap
metadata:
  name: tesla-wellknown
  labels:
    app: tesla-wellknown
data:
  # Replace with the real public key generated in runbook step 3
  # (docs/phase0-fleet-api-runbook.md). This key is public — no sops needed.
  com.tesla.3p.public-key.pem: |
    -----BEGIN PUBLIC KEY-----
    REPLACE_ME_WITH_REAL_PUBLIC_KEY
    -----END PUBLIC KEY-----
