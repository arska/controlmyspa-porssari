apiVersion: v1
kind: ConfigMap
metadata:
  name: mosquitto
  labels:
    app: mosquitto
data:
  # cluster-internal only (no Ingress/Route) — anonymous access acceptable
  mosquitto.conf: |
    listener 1883 0.0.0.0
    allow_anonymous true
    persistence false
